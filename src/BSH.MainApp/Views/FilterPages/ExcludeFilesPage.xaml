<Page
    x:Class="BSH.MainApp.Views.FilterPages.ExcludeFilesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid RowSpacing="12" Padding="8">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ListView Grid.Row="0"
                    ItemsSource="{Binding ExcludeFiles}"
                    SelectedItem="{Binding SelectedFile, Mode=TwoWay}">
            <ListView.Header>
                <TextBlock FontWeight="Bold">File</TextBlock>
            </ListView.Header>
        </ListView>
        <TextBlock Grid.Row="1"
                   Text="{Binding ValidationErrorMessage}"
                   Foreground="Red"
                   TextWrapping="Wrap">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Setter Property="Visibility" Value="Visible" />
                    <Style.Triggers>
                        <Trigger Property="Text" Value="">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </Trigger>
                        <Trigger Property="Text" Value="{x:Null}">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>

        <Grid Grid.Row="2">
            <StackPanel HorizontalAlignment="Right" Orientation="Horizontal" Spacing="10">
                <Button Content="Add File(s)" Command="{Binding BrowseFileCommand}" />
                <Button Content="Remove" Command="{Binding RemoveFileCommand}" />
            </StackPanel>
        </Grid>
    </Grid>
</Page>
