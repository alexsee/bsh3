<winex:WindowEx
    x:Class="BSH.MainApp.Windows.FilterWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:winex="using:WinUIEx"
    xmlns:viewmodels="using:BSH.MainApp.ViewModels.Windows"
    mc:Ignorable="d"
    Title="Exclude items"
    Height="650"
    Width="850"
    IsResizable="True"
    IsMaximizable="False"
    IsMinimizable="False"
    IsAlwaysOnTop="True">

    <Grid Background="White" d:DataContext="{d:DesignInstance Type=viewmodels:FilterViewModel}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="24" RowSpacing="8">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition />
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0" Spacing="8">
                <TextBlock FontSize="24" FontWeight="SemiBold">Exclude files and folders</TextBlock>
                <TextBlock>Exclude files, folders, file types and based on regular expressions. Select the files and folders you want to exclude in backups.</TextBlock>
            </StackPanel>

            <SelectorBar x:Name="FilterSelectorBar"
                         Grid.Row="1"
                         SelectionChanged="FilterSelectorBar_SelectionChanged">
                <SelectorBarItem x:Name="FilesItem" Text="Files" />
                <SelectorBarItem x:Name="FoldersItem" Text="Folders" />
                <SelectorBarItem x:Name="FileTypesItem" Text="File types" />
                <SelectorBarItem x:Name="RegexItem" Text="Regular expressions" />
            </SelectorBar>

            <Frame x:Name="ContentFrame" Grid.Row="2" Margin="0,12,0,0" />
        </Grid>

        <Grid Grid.Row="1" Background="#fafafa" Padding="30,10" BorderBrush="#f0f0f0" BorderThickness="0,1,0,0">
            <StackPanel Orientation="Horizontal" Spacing="10" HorizontalAlignment="Right">
                <Button Command="{x:Bind ViewModel.CancelCommand}">Cancel</Button>
                <Button Command="{x:Bind ViewModel.SaveCommand}" Style="{StaticResource AccentButtonStyle}">Save</Button>
            </StackPanel>
        </Grid>
    </Grid>
</winex:WindowEx>
