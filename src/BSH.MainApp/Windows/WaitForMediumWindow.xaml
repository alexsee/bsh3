<?xml version="1.0" encoding="utf-8"?>
<winex:WindowEx xmlns:winex="using:WinUIEx"
    x:Class="BSH.MainApp.Windows.WaitForMediumWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BSH.MainApp.Windows"
    xmlns:helpers="using:BSH.MainApp.Helpers"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="{helpers:ResourceString Name=WaitForMedium_Title}"
    Width="600"
    Height="300"
    IsMaximizable="False"
    IsMinimizable="False"
    IsResizable="False"
    IsAlwaysOnTop="True"
    Closed="WindowEx_Closed">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Progress Ring -->
        <StackPanel Margin="30" Orientation="Horizontal" VerticalAlignment="Center">
            <ProgressRing Width="30" Height="30" IsActive="True" Margin="0,0,20,0" />
            <TextBlock Text="{helpers:ResourceString Name=WaitForMedium_Heading}" Style="{StaticResource SubtitleTextBlockStyle}"
                       VerticalAlignment="Center" />
        </StackPanel>

        <!-- Description -->
        <TextBlock Grid.Row="1"
                   Text="{helpers:ResourceString Name=WaitForMedium_Text}"
                   TextWrapping="Wrap"
                   Margin="30,0,30,0" />

        <!-- Cancel Button -->
        <Grid Grid.Row="2" Background="#fafafa" Padding="30,15" BorderBrush="#f0f0f0" BorderThickness="0,1,0,0">
            <StackPanel Orientation="Horizontal" Spacing="10" HorizontalAlignment="Right">
                <Button Command="{x:Bind ViewModel.CancelCommand}" Content="{helpers:ResourceString Name=WaitForMedium_Cancel}" />
            </StackPanel>
        </Grid>
    </Grid>
</winex:WindowEx>
