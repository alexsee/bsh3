<?xml version="1.0" encoding="utf-8"?>
<winex:WindowEx
    x:Class="BSH.MainApp.Windows.SwitchStorageWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:BSH.MainApp.ViewModels.Windows"
    xmlns:winex="using:WinUIEx"
    Title="Switch storage"
    Width="640"
    Height="620"
    IsAlwaysOnTop="True"
    IsMaximizable="False"
    IsMinimizable="False"
    IsResizable="True"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" d:DataContext="{d:DesignInstance Type=viewmodels:SwitchStorageViewModel}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0" Padding="28,24,28,20" VerticalScrollBarVisibility="Auto">
            <StackPanel MaxWidth="560" HorizontalAlignment="Stretch" Spacing="20">
                <StackPanel Spacing="4">
                    <TextBlock FontSize="26" FontWeight="SemiBold" Text="Switch storage" />
                    <TextBlock
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        Text="Choose a new empty backup medium. Existing settings are kept, but backup history on this PC is cleared."
                        TextWrapping="WrapWholeWords" />
                </StackPanel>

                <ComboBox
                    Header="Medium type"
                    HorizontalAlignment="Stretch"
                    SelectedIndex="{x:Bind ViewModel.SelectedMediumIndex, Mode=TwoWay}">
                    <x:String>Local drive</x:String>
                    <x:String>FTP server</x:String>
                </ComboBox>

                <StackPanel Spacing="12" Visibility="{x:Bind ViewModel.LocalTargetVisibility, Mode=OneWay}">
                    <TextBlock FontWeight="SemiBold" Text="Select drive" />
                    <ListView
                        Height="220"
                        ItemsSource="{x:Bind ViewModel.AvailableDrives, Mode=OneWay}"
                        SelectedItem="{x:Bind ViewModel.SelectedDrive, Mode=TwoWay}"
                        DisplayMemberPath="DisplayName" />
                    <Button Command="{x:Bind ViewModel.RefreshDrivesCommand}" Content="Refresh drives" HorizontalAlignment="Left" />
                </StackPanel>

                <StackPanel Spacing="12" Visibility="{x:Bind ViewModel.FtpTargetVisibility, Mode=OneWay}">
                    <TextBlock FontWeight="SemiBold" Text="FTP server" />
                    <TextBox Header="Host" Text="{x:Bind ViewModel.FtpHost, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <TextBox Header="Port" Text="{x:Bind ViewModel.FtpPort, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <TextBox Header="Username" Text="{x:Bind ViewModel.FtpUser, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <PasswordBox Header="Password" Password="{Binding FtpPassword, Mode=TwoWay}" />
                    <TextBox Header="Path" Text="{x:Bind ViewModel.FtpFolder, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <ComboBox
                        Header="Encoding"
                        HorizontalAlignment="Stretch"
                        ItemsSource="{x:Bind ViewModel.FtpEncodings}"
                        SelectedItem="{x:Bind ViewModel.FtpEncoding, Mode=TwoWay}" />
                    <CheckBox Content="Enforce unencrypted connection" IsChecked="{x:Bind ViewModel.FtpEnforceUnencrypted, Mode=TwoWay}" />
                    <Button Command="{x:Bind ViewModel.TestFtpConnectionCommand}" Content="Test connection" HorizontalAlignment="Left" />
                </StackPanel>

                <TextBlock
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Text="{x:Bind ViewModel.ValidationErrorMessage, Mode=OneWay}"
                    TextWrapping="WrapWholeWords" />
            </StackPanel>
        </ScrollViewer>

        <Grid
            Grid.Row="1"
            Padding="28,12"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="0,1,0,0"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
            <StackPanel Orientation="Horizontal" Spacing="10" HorizontalAlignment="Right">
                <Button Command="{x:Bind ViewModel.CancelCommand}" Content="Cancel" />
                <Button Command="{x:Bind ViewModel.ConfirmCommand}" Content="Switch" Style="{StaticResource AccentButtonStyle}" />
            </StackPanel>
        </Grid>
    </Grid>
</winex:WindowEx>
